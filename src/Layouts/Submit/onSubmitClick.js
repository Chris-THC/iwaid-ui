import { statusCreated, statusOk } from "../../Layouts/HttpStatus/HTTPStatusCode";
import { GetTheAppContext } from "../../Context/AppContext";
export const addData= async({})=>{

    return async () => {
    //     const responseCreate = await createDateFunction;
      
    //   if (responseCreate.status === statusCreated) {
    //     await getAllDateDataFunction(setAllDataDate);
    //     setTextAlert("Agregado exitosamente");
    //     handleShowFloatAlter();  
    //   }else {
    //     setTextAlert("Error al agregar");
    //     handleShowFloatAlter();
    //   }
    }
}

export const editDate =({type})=>{
    return(
        <></>
    );
}

export const deleteData= ({type})=>{
    return(
        <></>
    );
}